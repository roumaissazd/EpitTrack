@model IEnumerable<EpitTrack.Models.coutrh>
@{
    ViewData["Title"] = "Index";
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Les Coûts RH</h3>
    </div>
    <!-- /.card-header -->
    <div class="card-body">
        <p>
            <a asp-action="Create"> + Ajouter Coût Rh</a>
        </p>

        <table id="example1" class="table table-bordered table-striped">
           
    <thead>
        <tr>
             <th>
                @Html.DisplayNameFor(model => model.prenom_rh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nom_rh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.date_deb_coutrh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.date_fin_coutrh)
            </th>
            <th>
               @Html.DisplayNameFor(model => model.mt_cout_paie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.mt_frais)
            </th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
           
            <td>
               @Html.DisplayFor(modelItem => item.prenom_rh)
            </td>
             <td>
               @Html.DisplayFor(modelItem => item.nom_rh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.date_deb_coutrh)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.date_fin_coutrh)
            </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.mt_cout_paie)
                        </td>
            <td>
                @Html.DisplayFor(modelItem => item.mt_frais)
            </td>
            <td>
                            <a asp-action="Edit" asp-route-id="@item.id_coutrh" class="btn btn-primary"><i class="fa fa-pencil-alt"></i>Edit</a>
                            <a asp-action="Details" asp-route-id="@item.id_coutrh" class="btn btn-secondary"><i class="fa fa-eye"></i>Details</a>
                            <a asp-action="Delete" asp-route-id="@item.id_coutrh" class="btn btn-danger"><i class="fa fa-trash"></i>Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>